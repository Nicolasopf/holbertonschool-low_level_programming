#include "holberton.h"

/**
 * print_buffer - prints a buffer.
 * @b: string to encode.
 * @size: size bytes of the buffer pointed.
 */

void print_buffer(char *b, int size)
{
	a = 1;
	_putchar(a);
}
