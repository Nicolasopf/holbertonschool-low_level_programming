#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: 0
 *
 */

int main(void)
{

int c = 50;

int b = 50;

while (c <= 59)
{
for ( ; b <= 59; b++)
{
putchar(c);
putchar(b);
if (b == 59 && c == 59)
{
putchar('\n');
break;
}
putchar(',');
putchar(' ');
}
b = 50;
c++;
}
return (0);
}
