#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Alphabet
 */

int main(void)
{
	int x;

	do 
	{
		for (x = 'a'; x <= 'z'; x++)
			putchar(x);
	} 
	while (x < 'z');
	do 
	{
		for (x = 'A'; x <= 'Z'; x++)
			putchar(x);
	}
	while (c < 'Z');
	putchar('\n');
	return (0);
}
